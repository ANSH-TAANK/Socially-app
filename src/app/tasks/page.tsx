// async function TasksPage() {
//   const response = await fetch("http://localhost:3000/api/tasks", {
//     cache: "no-store",
//   });
//   const tasks = await response.json();

//   console.log("tasks:", tasks);

//   return <div>TasksPage</div>;
// }
// export default TasksPage;


// //its not related to the project its just to learn the concept of api routes in nextjs
// //page.tsx in tasks folder and route.ts in api/tasks folder dont have any signifence for the project the both are for the learning purpose 